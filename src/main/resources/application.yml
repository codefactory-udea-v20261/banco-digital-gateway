spring:
  application:
    name: banco-digital-gateway
  profiles:
    active: ${APP_PROFILE:local}

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}

  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:4200}
            allowedMethods: GET,POST,PUT,DELETE,OPTIONS
            allowedHeaders: '*'
            maxAge: 3600

    discovery:
      client:
        serviceIdToZoneMapping:
          identity-service: default
          core-banking-service: default
          audit-service: default
          reporting-service: default

    loadbalancer:
      ribbon:
        enabled: false

server:
  port: ${PORT:${APP_PORT:8000}}
  error:
    include-message: never
    include-stacktrace: never
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized
  metrics:
    export:
      prometheus:
        enabled: true

# SpringDoc OpenAPI / Swagger Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    display-request-duration: true
    doc-expansion: list
    show-extensions: true
    urls:
      - name: 0. API Gateway
        url: /v3/api-docs
      - name: 1. Identity Service
        url: /api/v1/auth/api-docs
      - name: 2. Core Banking
        url: /api/v1/clientes/api-docs
      - name: 3. Audit Service
        url: /api/v1/audit/api-docs
      - name: 4. Reporting Service
        url: /api/v1/reportes/api-docs
  show-actuator: true
  
# Tracing Configuration
spring.zipkin:
  base-url: ${ZIPKIN_URL:http://localhost:9411/}
  enabled: true
spring.sleuth:
  sampler:
    probability: 1.0

# Resilience4j Configuration
resilience4j:
  circuitbreaker:
    instances:
      identity-service:
        registerHealthIndicator: true
        slidingWindowSize: 10
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: 2000
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 5000
        eventConsumerBufferSize: 10
      core-banking-service:
        registerHealthIndicator: true
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 5000

  ratelimiter:
    instances:
      api-rate-limit:
        registerHealthIndicator: false
        limitRefreshPeriod: 1m
        limitForPeriod: 100
        timeoutDuration: 5s

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:4200}

services:
  identity:
    url: ${IDENTITY_SERVICE_URL:http://identity:8080}
  core-banking:
    url: ${CORE_BANKING_SERVICE_URL:http://core-banking:8080}
  audit:
    url: ${AUDIT_SERVICE_URL:http://audit:8080}
  reporting:
    url: ${REPORTING_SERVICE_URL:http://reporting:8080}
